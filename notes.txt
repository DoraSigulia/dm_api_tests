Трудно найти, легко потерять и НЕЛЬЗЯ забывать!
Поэтому в этом файле будут представлены заметки по курсу и доп.материалам.

1. Python
-  Два типа наименования переменных: camel case (userName) и underscore notation (user_name).
-   \\: позволяет добавить внутрь строки слеш
    \n: осуществляет переход на новую строку
    \t: добавляет табуляцию (4 отступа)
- Если внутри строки встречается  "\n" и это не переход на новую строку, то перед строкой стоит указать символ r
- Вставка значений в строку - f
- Переменная не привязана жестко к определенному типу
- type() - узнать тип переменной



